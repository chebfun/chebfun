# dom = [-pi pi];
# N = chebop(@(x, u) -diff(u, 2) + 5*cos(2*x)*u, dom);
# N.bc = 'periodic';
# [V, D] = eigs(N, 6);
'Mathieu equation'
'scalar'
domain = '[-pi pi]';
DE = '-u'''' + 5*cos(2*x)*u = lambda*u';
BC = 'periodic';
tol = '1e-10';
type = 'eig';
